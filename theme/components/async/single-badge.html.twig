{% async:content %}
    {{ include('partials/components/store/item/badges/' ~ template, { label, shouldAnnounce } ) }}
{%  endasync %}

{% schema %}
{
    "template": {
        type": "string"
    },
    "label": {
        "type": "string"
    },
    "shouldAnnounce": {
        "type": "bool",
        "optional": true
    }
}
{% endschema %}