<iframe
    ref="iframe"
    src="{{ mapConfig|embed_map_url }}"
    allowtransparency="true"
    frameborder="0"
    scrolling="no"
    loading="lazy"
    width="100%"
    height="{{ mapConfig.height|default('230px') }}"
>
</iframe>