{% set badgeSize = size|default('normal') %}
{% set badgeShape = themeBadges.styles[themeBadges.types.low_inventory.variant].shape.id|default(shape) %}
{% set badgeVariant = themeBadges.styles[themeBadges.types.low_inventory.variant].fill_style|default(variant) %}

{{ include('partials/ui/badge', { label, type: 'low-stock', size: badgeSize, shape: badgeShape, variant: badgeVariant, badgeClasses, shouldAnnounce }) }}